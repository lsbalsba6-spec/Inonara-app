[1mdiff --git a/frontend/src/pages/Atlas.jsx b/frontend/src/pages/Atlas.jsx[m
[1mindex b94c9dd..fb85646 100644[m
[1m--- a/frontend/src/pages/Atlas.jsx[m
[1m+++ b/frontend/src/pages/Atlas.jsx[m
[36m@@ -20,26 +20,10 @@[m [mimport { getHistoricalDataSource, isPilotV3Enabled } from "../lib/featureFlags";[m
 import DevDataSourceIndicator from "../components/DevDataSourceIndicator";[m
 import { adaptHistoricalEntitiesToLegacyShape } from "../lib/historicalEntityAdapter";[m
 import { buildPilotV3Markers, buildFangProcessDisplay } from "../lib/pilotV3Adapter";[m
[32m+[m[32mimport { getMigrationVisualStyle, selectNonOverlappingLabels } from "../lib/atlasDisplay";[m
 import PilotV3InfoPanel from "../components/PilotV3InfoPanel";[m
 [m
[31m-// Milestone markers shown along the non-linear slider track, so users stay[m
[31m-// oriented even though early (geological) time is heavily compressed.[m
[31m-// Visual distinction by migration type: forced (solid, thick — a chattel/[m
[31m-// coerced displacement), voluntary (fine dotted — a matter of individual[m
[31m-// choice, even under hardship), mixed (dash-dot — a route carrying both),[m
[31m-// conquest (long dashes — military/political expansion, not a personal[m
[31m-// migration decision at all). See PR: "difference between forced and[m
[31m-// voluntary migration, varying honestly by period per the data found."[m
[31m-function dashPatternForMigrationType(type) {[m
[31m-  switch (type) {[m
[31m-    case "forced": return undefined; // solid[m
[31m-    case "voluntary": return "2 4";[m
[31m-    case "mixed": return "8 3 2 3";[m
[31m-    case "conquest": return "12 4";[m
[31m-    default: return "5 5"; // unclassified/legacy routes without a migration_type yet[m
[31m-  }[m
[31m-}[m
[31m-[m
[32m+[m[32m// Milestone markers shown along the non-linear slider track.[m
 const MILESTONES = [[m
   { year: -300000000, label: "Pangée" },[m
   { year: -66000000, label: "Fin des dinosaures" },[m
[36m@@ -156,6 +140,53 @@[m [mconst Atlas = () => {[m
     return best;[m
   }, [mode, plateEpochs, year]);[m
 [m
[32m+[m
[32m+[m[32m  const visibleMapLabels = useMemo(() => {[m
[32m+[m[32m    if (mode !== "historical" || !project) return [];[m
[32m+[m
[32m+[m[32m    const candidates = [];[m
[32m+[m
[32m+[m[32m    if (showPolities) {[m
[32m+[m[32m      for (const polity of visiblePolities) {[m
[32m+[m[32m        const point = project(polity.coords[0], polity.coords[1]);[m
[32m+[m[32m        if (!point) continue;[m
[32m+[m[32m        const radius = Math.max(1.2, Math.max(4, Math.min(14, polity.radius_km / 90)) / zoomScale);[m
[32m+[m[32m        const importance = Number(polity.importance ?? polity.radius_km ?? 0);[m
[32m+[m[32m        const revealThreshold = zoomScale <= 1.25 ? 900 : zoomScale <= 2 ? 520 : 180;[m
[32m+[m[32m        if (importance * zoomScale < revealThreshold) continue;[m
[32m+[m[32m        candidates.push({[m
[32m+[m[32m          id: `polity-${polity.id}`,[m
[32m+[m[32m          text: polity.name,[m
[32m+[m[32m          x: point[0],[m
[32m+[m[32m          y: point[1] - radius - 4,[m
[32m+[m[32m          fontSize: Math.max(7, 10 / Math.max(1, zoomScale * 0.55)),[m
[32m+[m[32m          priority: 300 + Math.min(importance, 2000) / 20,[m
[32m+[m[32m          opacity: 1,[m
[32m+[m[32m        });[m
[32m+[m[32m      }[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    for (const marker of pilotV3Markers) {[m
[32m+[m[32m      const point = project(marker.coords[0], marker.coords[1]);[m
[32m+[m[32m      if (!point) continue;[m
[32m+[m[32m      const size = Math.max(4, 10 / zoomScale);[m
[32m+[m[32m      candidates.push({[m
[32m+[m[32m        id: `pilot-${marker.id}`,[m
[32m+[m[32m        text: marker.primaryName.value,[m
[32m+[m[32m        x: point[0],[m
[32m+[m[32m        y: point[1] - size - 4,[m
[32m+[m[32m        fontSize: Math.max(7, 10 / Math.max(1, zoomScale)),[m
[32m+[m[32m        priority: 500,[m
[32m+[m[32m        opacity: marker.style.opacity,[m
[32m+[m[32m      });[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    return selectNonOverlappingLabels(candidates, {[m
[32m+[m[32m      padding: zoomScale < 1.5 ? 8 : 4,[m
[32m+[m[32m      maxLabels: zoomScale < 1.5 ? 32 : zoomScale < 3 ? 58 : 90,[m
[32m+[m[32m    });[m
[32m+[m[32m  }, [mode, project, showPolities, visiblePolities, pilotV3Markers, zoomScale]);[m
[32m+[m
   const toPolyPoints = (coords) => {[m
     if (!project) return "";[m
     return coords[m
[36m@@ -242,6 +273,7 @@[m [mconst Atlas = () => {[m
           {mode !== "geological" && project && visibleRoutes.map((r) => {[m
             if (activeRoutes[r.id] === false) return null;[m
             const isSelected = selected?.kind === "route" && selected?.id === r.id;[m
[32m+[m[32m            const routeStyle = getMigrationVisualStyle(r);[m
             return ([m
               <g key={r.id} onClick={() => setSelected({ kind: "route", ...r })} style={{ cursor: "pointer" }}>[m
                 <polyline[m
[36m@@ -263,9 +295,9 @@[m [mconst Atlas = () => {[m
                 <polyline[m
                   points={toPolyPoints(r.points)}[m
                   fill="none"[m
[31m-                  stroke={r.color}[m
[31m-                  strokeWidth={(isSelected ? 3.2 : 2.2) + (r.migration_type === "forced" ? 0.8 : 0)}[m
[31m-                  strokeDasharray={dashPatternForMigrationType(r.migration_type)}[m
[32m+[m[32m                  stroke={routeStyle.color}[m
[32m+[m[32m                  strokeWidth={routeStyle.width + (isSelected ? 1 : 0)}[m
[32m+[m[32m                  strokeDasharray={routeStyle.dashArray}[m
                   opacity={isSelected ? 1 : 0.88}[m
                   style={{ pointerEvents: "none" }}[m
                 />[m
[36m@@ -358,43 +390,28 @@[m [mconst Atlas = () => {[m
             );[m
           })}[m
 [m
[31m-          {/* LABELS PASS — always rendered last, on top of every shape above. */}[m
[31m-          {mode === "historical" && showPolities && project && visiblePolities.map((p) => {[m
[31m-            const c = project(p.coords[0], p.coords[1]);[m
[31m-            if (!c) return null;[m
[31m-            const r = Math.max(1.2, Math.max(4, Math.min(14, p.radius_km / 90)) / zoomScale);[m
[31m-            // Zoom-based label reveal: at zoom=1 only the largest territories[m
[31m-            // show their name; smaller ones appear progressively as the user[m
[31m-            // zooms in, so labels don't overlap into unreadable clutter.[m
[31m-            const showLabel = p.radius_km * zoomScale > 700;[m
[31m-            if (!showLabel) return null;[m
[31m-            return ([m
[31m-              <text key={`label-${p.id}`} x={c[0]} y={c[1] - r - 4} fontSize={9 / Math.max(1, zoomScale * 0.6)} fill={ATLAS_COLORS.textBone} textAnchor="middle" style={{ fontFamily: "serif", pointerEvents: "none" }}>[m
[31m-                {p.name}[m
[31m-              </text>[m
[31m-            );[m
[31m-          })}[m
[31m-[m
[31m-          {mode === "historical" && project && pilotV3Markers.map((marker) => {[m
[31m-            const c = project(marker.coords[0], marker.coords[1]);[m
[31m-            if (!c) return null;[m
[31m-            const size = Math.max(4, 10 / zoomScale);[m
[31m-            const { style } = marker;[m
[31m-            return ([m
[31m-              <text[m
[31m-                key={`label-${marker.id}`}[m
[31m-                x={c[0]}[m
[31m-                y={c[1] - size - 4}[m
[31m-                fontSize={10 / zoomScale}[m
[31m-                fill={ATLAS_COLORS.textBone}[m
[31m-                textAnchor="middle"[m
[31m-                opacity={style.opacity}[m
[31m-                style={{ fontFamily: "serif", pointerEvents: "none" }}[m
[31m-              >[m
[31m-                {marker.primaryName.value}[m
[31m-              </text>[m
[31m-            );[m
[31m-          })}[m
[32m+[m[32m          {/* LABELS PASS — collision-filtered and rendered after every shape. */}[m
[32m+[m[32m          {mode === "historical" && visibleMapLabels.map((label) => ([m
[32m+[m[32m            <text[m
[32m+[m[32m              key={label.id}[m
[32m+[m[32m              x={label.x}[m
[32m+[m[32m              y={label.y}[m
[32m+[m[32m              fontSize={label.fontSize}[m
[32m+[m[32m              fill={ATLAS_COLORS.textBone}[m
[32m+[m[32m              textAnchor="middle"[m
[32m+[m[32m              opacity={label.opacity}[m
[32m+[m[32m              style={{[m
[32m+[m[32m                fontFamily: "serif",[m
[32m+[m[32m                pointerEvents: "none",[m
[32m+[m[32m                paintOrder: "stroke",[m
[32m+[m[32m                stroke: "rgba(26, 22, 20, 0.88)",[m
[32m+[m[32m                strokeWidth: 2.5,[m
[32m+[m[32m                strokeLinejoin: "round",[m
[32m+[m[32m              }}[m
[32m+[m[32m            >[m
[32m+[m[32m              {label.text}[m
[32m+[m[32m            </text>[m
[32m+[m[32m          ))}[m
 [m
           {/* SELECTED-MARKER HIGHLIGHT — a pulsing bright ring around[m
               whichever point is currently open in the detail panel, so it's[m
[36m@@ -547,7 +564,7 @@[m [mconst Atlas = () => {[m
                       className="accent-gold"[m
                       data-testid={`route-toggle-${r.id}`}[m
                     />[m
[31m-                    <span className="w-6 h-[2px]" style={{ background: r.color }} />[m
[32m+[m[32m                    <span className="w-6 h-[2px]" style={{ background: getMigrationVisualStyle(r).color }} />[m
                     <span className="text-bone/80 text-xs">{r.name}</span>[m
                   </label>[m
                 ))}[m
[36m@@ -558,19 +575,19 @@[m [mconst Atlas = () => {[m
               <div className="space-y-2">[m
                 <p className="text-bone/60 text-[0.65rem] uppercase tracking-wider mb-1">Style des lignes migratoires</p>[m
                 <div className="flex items-center gap-3">[m
[31m-                  <span className="w-6 h-[2px]" style={{ background: "#7B2D26" }} />[m
[32m+[m[32m                  <span className="w-6 h-[2px]" style={{ background: getMigrationVisualStyle("forced").color }} />[m
                   <span className="text-bone/80 text-xs">Migration forcée (trait plein, plus épais)</span>[m
                 </div>[m
                 <div className="flex items-center gap-3">[m
[31m-                  <span className="w-6 h-[2px]" style={{ background: "#4ade80", borderTop: "2px dotted #4ade80", height: 0 }} />[m
[32m+[m[32m                  <span className="w-6 h-[2px]" style={{ borderTop: `2px dotted ${getMigrationVisualStyle("voluntary").color}`, height: 0 }} />[m
                   <span className="text-bone/80 text-xs">Migration volontaire (finement pointillé)</span>[m
                 </div>[m
                 <div className="flex items-center gap-3">[m
[31m-                  <span className="w-6" style={{ borderTop: `2px dashed ${ATLAS_COLORS.amber}` }} />[m
[32m+[m[32m                  <span className="w-6" style={{ borderTop: `2px dashed ${getMigrationVisualStyle("mixed").color}` }} />[m
                   <span className="text-bone/80 text-xs">Mixte (tirets-points)</span>[m
                 </div>[m
                 <div className="flex items-center gap-3">[m
[31m-                  <span className="w-6" style={{ borderTop: "2px dashed #9CA3AF" }} />[m
[32m+[m[32m                  <span className="w-6" style={{ borderTop: `2px dashed ${getMigrationVisualStyle("conquest").color}` }} />[m
                   <span className="text-bone/80 text-xs">Conquête / expansion militaire (longs tirets)</span>[m
                 </div>[m
                 <div className="h-px bg-[#2A2421] my-2" />[m
[36m@@ -599,7 +616,7 @@[m [mconst Atlas = () => {[m
                       className="accent-gold"[m
                       data-testid={`route-toggle-${r.id}`}[m
                     />[m
[31m-                    <span className="w-6 h-[2px]" style={{ background: r.color }} />[m
[32m+[m[32m                    <span className="w-6 h-[2px]" style={{ background: getMigrationVisualStyle(r).color }} />[m
                     <span className="text-bone/80 text-xs">{r.name}</span>[m
                   </label>[m
                 ))}[m
[36m@@ -627,7 +644,7 @@[m [mconst Atlas = () => {[m
                               className="accent-gold"[m
                               data-testid={`route-toggle-${r.id}`}[m
                             />[m
[31m-                            <span className="w-6 h-[2px] shrink-0" style={{ background: r.color }} />[m
[32m+[m[32m                            <span className="w-6 h-[2px] shrink-0" style={{ background: getMigrationVisualStyle(r).color }} />[m
                             <span className="text-bone/80 text-[0.65rem] leading-tight">{r.name}</span>[m
                           </label>[m
                         ))}[m
[36m@@ -644,7 +661,7 @@[m [mconst Atlas = () => {[m
         {selected && ([m
           <div className="absolute bottom-28 left-1/2 -translate-x-1/2 z-[600] glass w-[92%] max-w-[420px] p-5" data-testid="marker-detail-card">[m
             <button onClick={() => setSelected(null)} className="absolute top-3 right-4 text-bone/50 hover:text-bone text-lg" data-testid="close-detail">×</button>[m
[31m-            <p className="overline text-[0.6rem]" style={{ color: selected.color || ATLAS_COLORS.gold }}>[m
[32m+[m[32m            <p className="overline text-[0.6rem]" style={{ color: selected.kind === "route" ? getMigrationVisualStyle(selected).color : (selected.color || ATLAS_COLORS.gold) }}>[m
               {selected.kind === "civ" && `Civilisation · ${t(`region.${selected.region}`)}`}[m
               {selected.kind === "place" && `${selected.type} · ${selected.era}`}[m
               {selected.kind === "diaspora" && `Diaspora · ${t(`region.${selected.region}`)}`}[m
[36m@@ -660,11 +677,11 @@[m [mconst Atlas = () => {[m
               <span[m
                 className="inline-block mt-2 text-[0.6rem] uppercase tracking-wider px-2 py-0.5 rounded-full border"[m
                 style={{[m
[31m-                  borderColor: { forced: "#7B2D26", voluntary: "#4ade80", mixed: ATLAS_COLORS.amber, conquest: "#9CA3AF" }[selected.migration_type] || "#9CA3AF",[m
[31m-                  color: { forced: "#7B2D26", voluntary: "#4ade80", mixed: ATLAS_COLORS.amber, conquest: "#9CA3AF" }[selected.migration_type] || "#9CA3AF",[m
[32m+[m[32m                  borderColor: getMigrationVisualStyle(selected).color,[m
[32m+[m[32m                  color: getMigrationVisualStyle(selected).color,[m
                 }}[m
               >[m
[31m-                {{ forced: "Migration forcée", voluntary: "Migration volontaire", mixed: "Migration mixte", conquest: "Conquête / expansion militaire" }[selected.migration_type]}[m
[32m+[m[32m                {getMigrationVisualStyle(selected).label}[m
               </span>[m
             )}[m
             <p className="text-bone/70 text-sm mt-2 leading-relaxed">[m

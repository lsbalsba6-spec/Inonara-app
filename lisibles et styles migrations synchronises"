[1mdiff --git a/frontend/src/pages/Atlas.jsx b/frontend/src/pages/Atlas.jsx[m
[1mindex b94c9dd..7d81714 100644[m
[1m--- a/frontend/src/pages/Atlas.jsx[m
[1m+++ b/frontend/src/pages/Atlas.jsx[m
[36m@@ -20,6 +20,7 @@[m [mimport { getHistoricalDataSource, isPilotV3Enabled } from "../lib/featureFlags";[m
 import DevDataSourceIndicator from "../components/DevDataSourceIndicator";[m
 import { adaptHistoricalEntitiesToLegacyShape } from "../lib/historicalEntityAdapter";[m
 import { buildPilotV3Markers, buildFangProcessDisplay } from "../lib/pilotV3Adapter";[m
[32m+[m[32mimport { getMigrationVisualStyle, selectNonOverlappingLabels } from "../lib/atlasDisplay";[m
 import PilotV3InfoPanel from "../components/PilotV3InfoPanel";[m
 [m
 // Milestone markers shown along the non-linear slider track, so users stay[m
[36m@@ -30,16 +31,6 @@[m [mimport PilotV3InfoPanel from "../components/PilotV3InfoPanel";[m
 // conquest (long dashes — military/political expansion, not a personal[m
 // migration decision at all). See PR: "difference between forced and[m
 // voluntary migration, varying honestly by period per the data found."[m
[31m-function dashPatternForMigrationType(type) {[m
[31m-  switch (type) {[m
[31m-    case "forced": return undefined; // solid[m
[31m-    case "voluntary": return "2 4";[m
[31m-    case "mixed": return "8 3 2 3";[m
[31m-    case "conquest": return "12 4";[m
[31m-    default: return "5 5"; // unclassified/legacy routes without a migration_type yet[m
[31m-  }[m
[31m-}[m
[31m-[m
 const MILESTONES = [[m
   { year: -300000000, label: "Pangée" },[m
   { year: -66000000, label: "Fin des dinosaures" },[m
[36m@@ -142,6 +133,36 @@[m [mconst Atlas = () => {[m
     [routes, year][m
   );[m
 [m
[32m+[m[32m  const atlasLabels = useMemo(() => {[m
[32m+[m[32m    if (mode !== "historical" || !project) return [];[m
[32m+[m[32m    const candidates = [];[m
[32m+[m
[32m+[m[32m    if (showPolities) {[m
[32m+[m[32m      visiblePolities.forEach((p) => {[m
[32m+[m[32m        const point = project(p.coords[0], p.coords[1]);[m
[32m+[m[32m        if (!point) return;[m
[32m+[m[32m        const importance = Math.max(0, Number(p.radius_km) || 0);[m
[32m+[m[32m        const minimumImportance = zoomScale < 1.5 ? 950 : zoomScale < 2.5 ? 450 : 150;[m
[32m+[m[32m        if (importance < minimumImportance) return;[m
[32m+[m[32m        candidates.push({[m
[32m+[m[32m          id: `polity-${p.id}`, text: p.name, x: point[0], y: point[1],[m
[32m+[m[32m          priority: 3000 + importance, screenFontSize: 13, offsetYScreen: -12, kind: "polity",[m
[32m+[m[32m        });[m
[32m+[m[32m      });[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    pilotV3Markers.forEach((marker) => {[m
[32m+[m[32m      const point = project(marker.coords[0], marker.coords[1]);[m
[32m+[m[32m      if (!point) return;[m
[32m+[m[32m      candidates.push({[m
[32m+[m[32m        id: `pilot-${marker.id}`, text: marker.primaryName.value, x: point[0], y: point[1],[m
[32m+[m[32m        priority: 5000, screenFontSize: 12, offsetYScreen: -14, kind: "pilot", opacity: marker.style.opacity,[m
[32m+[m[32m      });[m
[32m+[m[32m    });[m
[32m+[m
[32m+[m[32m    return selectNonOverlappingLabels(candidates, zoomScale);[m
[32m+[m[32m  }, [mode, project, showPolities, visiblePolities, pilotV3Markers, zoomScale]);[m
[32m+[m
   const currentEpoch = useMemo(() => {[m
     if (mode !== "geological" || plateEpochs.length === 0) return null;[m
     let best = plateEpochs[0];[m
[36m@@ -242,6 +263,7 @@[m [mconst Atlas = () => {[m
           {mode !== "geological" && project && visibleRoutes.map((r) => {[m
             if (activeRoutes[r.id] === false) return null;[m
             const isSelected = selected?.kind === "route" && selected?.id === r.id;[m
[32m+[m[32m            const routeStyle = getMigrationVisualStyle(r.migration_type);[m
             return ([m
               <g key={r.id} onClick={() => setSelected({ kind: "route", ...r })} style={{ cursor: "pointer" }}>[m
                 <polyline[m
[36m@@ -263,9 +285,9 @@[m [mconst Atlas = () => {[m
                 <polyline[m
                   points={toPolyPoints(r.points)}[m
                   fill="none"[m
[31m-                  stroke={r.color}[m
[31m-                  strokeWidth={(isSelected ? 3.2 : 2.2) + (r.migration_type === "forced" ? 0.8 : 0)}[m
[31m-                  strokeDasharray={dashPatternForMigrationType(r.migration_type)}[m
[32m+[m[32m                  stroke={routeStyle.color}[m
[32m+[m[32m                  strokeWidth={isSelected ? routeStyle.width + 1 : routeStyle.width}[m
[32m+[m[32m                  strokeDasharray={routeStyle.dasharray}[m
                   opacity={isSelected ? 1 : 0.88}[m
                   style={{ pointerEvents: "none" }}[m
                 />[m
[36m@@ -358,43 +380,25 @@[m [mconst Atlas = () => {[m
             );[m
           })}[m
 [m
[31m-          {/* LABELS PASS — always rendered last, on top of every shape above. */}[m
[31m-          {mode === "historical" && showPolities && project && visiblePolities.map((p) => {[m
[31m-            const c = project(p.coords[0], p.coords[1]);[m
[31m-            if (!c) return null;[m
[31m-            const r = Math.max(1.2, Math.max(4, Math.min(14, p.radius_km / 90)) / zoomScale);[m
[31m-            // Zoom-based label reveal: at zoom=1 only the largest territories[m
[31m-            // show their name; smaller ones appear progressively as the user[m
[31m-            // zooms in, so labels don't overlap into unreadable clutter.[m
[31m-            const showLabel = p.radius_km * zoomScale > 700;[m
[31m-            if (!showLabel) return null;[m
[31m-            return ([m
[31m-              <text key={`label-${p.id}`} x={c[0]} y={c[1] - r - 4} fontSize={9 / Math.max(1, zoomScale * 0.6)} fill={ATLAS_COLORS.textBone} textAnchor="middle" style={{ fontFamily: "serif", pointerEvents: "none" }}>[m
[31m-                {p.name}[m
[31m-              </text>[m
[31m-            );[m
[31m-          })}[m
[31m-[m
[31m-          {mode === "historical" && project && pilotV3Markers.map((marker) => {[m
[31m-            const c = project(marker.coords[0], marker.coords[1]);[m
[31m-            if (!c) return null;[m
[31m-            const size = Math.max(4, 10 / zoomScale);[m
[31m-            const { style } = marker;[m
[31m-            return ([m
[31m-              <text[m
[31m-                key={`label-${marker.id}`}[m
[31m-                x={c[0]}[m
[31m-                y={c[1] - size - 4}[m
[31m-                fontSize={10 / zoomScale}[m
[31m-                fill={ATLAS_COLORS.textBone}[m
[31m-                textAnchor="middle"[m
[31m-                opacity={style.opacity}[m
[31m-                style={{ fontFamily: "serif", pointerEvents: "none" }}[m
[31m-              >[m
[31m-                {marker.primaryName.value}[m
[31m-              </text>[m
[31m-            );[m
[31m-          })}[m
[32m+[m[32m          {/* LABELS PASS — collision-filtered in screen space. */}[m
[32m+[m[32m          {mode === "historical" && atlasLabels.map((label) => ([m
[32m+[m[32m            <text[m
[32m+[m[32m              key={label.id}[m
[32m+[m[32m              x={label.x}[m
[32m+[m[32m              y={label.renderY}[m
[32m+[m[32m              fontSize={label.rawFontSize}[m
[32m+[m[32m              fill={ATLAS_COLORS.textBone}[m
[32m+[m[32m              textAnchor="middle"[m
[32m+[m[32m              opacity={label.opacity ?? 1}[m
[32m+[m[32m              paintOrder="stroke"[m
[32m+[m[32m              stroke={ATLAS_COLORS.ocean}[m
[32m+[m[32m              strokeWidth={3 / Math.max(1, zoomScale)}[m
[32m+[m[32m              strokeLinejoin="round"[m
[32m+[m[32m              style={{ fontFamily: "serif", pointerEvents: "none" }}[m
[32m+[m[32m            >[m
[32m+[m[32m              {label.text}[m
[32m+[m[32m            </text>[m
[32m+[m[32m          ))}[m
 [m
           {/* SELECTED-MARKER HIGHLIGHT — a pulsing bright ring around[m
               whichever point is currently open in the detail panel, so it's[m
[36m@@ -547,7 +551,7 @@[m [mconst Atlas = () => {[m
                       className="accent-gold"[m
                       data-testid={`route-toggle-${r.id}`}[m
                     />[m
[31m-                    <span className="w-6 h-[2px]" style={{ background: r.color }} />[m
[32m+[m[32m                    <span className="w-6 h-[2px]" style={{ background: getMigrationVisualStyle(r.migration_type).color }} />[m
                     <span className="text-bone/80 text-xs">{r.name}</span>[m
                   </label>[m
                 ))}[m
[36m@@ -599,7 +603,7 @@[m [mconst Atlas = () => {[m
                       className="accent-gold"[m
                       data-testid={`route-toggle-${r.id}`}[m
                     />[m
[31m-                    <span className="w-6 h-[2px]" style={{ background: r.color }} />[m
[32m+[m[32m                    <span className="w-6 h-[2px]" style={{ background: getMigrationVisualStyle(r.migration_type).color }} />[m
                     <span className="text-bone/80 text-xs">{r.name}</span>[m
                   </label>[m
                 ))}[m
[36m@@ -627,7 +631,7 @@[m [mconst Atlas = () => {[m
                               className="accent-gold"[m
                               data-testid={`route-toggle-${r.id}`}[m
                             />[m
[31m-                            <span className="w-6 h-[2px] shrink-0" style={{ background: r.color }} />[m
[32m+[m[32m                            <span className="w-6 h-[2px] shrink-0" style={{ background: getMigrationVisualStyle(r.migration_type).color }} />[m
                             <span className="text-bone/80 text-[0.65rem] leading-tight">{r.name}</span>[m
                           </label>[m
                         ))}[m
[36m@@ -660,11 +664,11 @@[m [mconst Atlas = () => {[m
               <span[m
                 className="inline-block mt-2 text-[0.6rem] uppercase tracking-wider px-2 py-0.5 rounded-full border"[m
                 style={{[m
[31m-                  borderColor: { forced: "#7B2D26", voluntary: "#4ade80", mixed: ATLAS_COLORS.amber, conquest: "#9CA3AF" }[selected.migration_type] || "#9CA3AF",[m
[31m-                  color: { forced: "#7B2D26", voluntary: "#4ade80", mixed: ATLAS_COLORS.amber, conquest: "#9CA3AF" }[selected.migration_type] || "#9CA3AF",[m
[32m+[m[32m                  borderColor: getMigrationVisualStyle(selected.migration_type).color,[m
[32m+[m[32m                  color: getMigrationVisualStyle(selected.migration_type).color,[m
                 }}[m
               >[m
[31m-                {{ forced: "Migration forcée", voluntary: "Migration volontaire", mixed: "Migration mixte", conquest: "Conquête / expansion militaire" }[selected.migration_type]}[m
[32m+[m[32m                {getMigrationVisualStyle(selected.migration_type).label}[m
               </span>[m
             )}[m
             <p className="text-bone/70 text-sm mt-2 leading-relaxed">[m

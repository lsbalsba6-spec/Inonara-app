# Correctif build V5.1 — cartes Afrique du Sud

- Stabilise la valeur de padding utilisée par `FitBounds`.
- Ajoute `padding` aux dépendances du `useEffect`.
- Corrige l'erreur ESLint traitée comme bloquante par Vercel CI.
- Aucun contenu historique ou visuel n'est modifié.
